name: 'Auto_Fork_PR_Generator'
description: 'Auto Fork PR Generator'
inputs:
  github-token:
    description: 'github token'
    required: true
outputs:
  time: # output will be available to future steps
    description: 'The current time after waiting'
runs:
  using: 'node12'
  main: 'dist/index.js'
